{
  "name": "e2e-staking-dashboard",
  "scripts": {
    "test": "env NODE_ENV=production nightwatch --reporter html-reporter.js",
    "serve": "env NODE_ENV=production node ./server.js",
    "test:dev": "env NODE_ENV=development nightwatch --reporter html-reporter.js",
    "serve:dev": "env NODE_ENV=development node ./server.js"
  },
  "devDependencies": {
    "chromedriver": "^2.36.0",
    "handlebars": "^4.7.3",
    "nightwatch": "^0.9.20"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "dotenv-expand": "^5.1.0",
    "express": "^4.17.1",
    "recursive-readdir-synchronous": "0.0.4",
    "selenium-webdriver": "^4.0.0-alpha.7"
  }
}
