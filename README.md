# e2e integration test for staking-dashboard

## Installation
`npm i`

## Start Docker locally or use remote Selenium hub
`docker-compose up -d`

## Start tests
`npm run test`

## Generate report
`npm run report`
